spring.application.name=SpringFirstProject

# PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/SpringFirstProject
spring.datasource.username=postgres
spring.datasource.password=Sb202582


# JPA/Hibernate
#spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.cache.use_second_level_cache=false

# Habilita la ejecución de data.sql
spring.sql.init.mode=always

# Thymeleaf
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.mode=HTML
spring.thymeleaf.cache=false

# server
server.error.whitelabel.enabled=false
server.port=8083

# Actuator
management.endpoints.web.exposure.include=health,info,metrics,env,beans,loggers,httpexchanges
management.endpoints.web.base-path=/actuator
management.endpoint.health.show-details=always

# Swagger
springdoc.show-actuator=true
springdoc.use-management-port=true
management.endpoints.web.exposure.include=openapi,swagger-ui
management.server.port=9090
springdoc.api-docs.path=/v3/api-docs

# Idioma
spring.messages.basename=messages
spring.messages.encoding=UTF-8
spring.messages.fallback-to-system-locale=false

# Mostrar parámetros de consulta SQL
#logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

#Gmail
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=dossantoshPrueba@gmail.com             # EL USUARIO SMTP (ej. tu correo)
spring.mail.password=kqfh dayf uokf mxdj                 # LA CONTRASEÑA o app password de Gmail

# Autenticación y cifrado
spring.mail.properties.mail.smtp.auth=true                  # Habilita autenticación SMTP
spring.mail.properties.mail.smtp.starttls.enable=true       # Habilita STARTTLS
spring.mail.properties.mail.smtp.starttls.required=true     # Lo exige antes de autenticar
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com   # Confía en el certificado de Gmail
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

spring.mail.default-encoding=UTF-8